dags:
  gitSync:
    enabled: true
    repo: https://github.com/rahulkatlaparthi/airflow-dags.git
    branch: test
    subPath: dags

webserver:
  service:
    type: LoadBalancer
  defaultUser:
    enabled: true
    username: admin
    password: admin
